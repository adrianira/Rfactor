% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ei30.R
\name{rf_calculate_ei30}
\alias{rf_calculate_ei30}
\title{Calculate event rainfall erosivity and EI30}
\usage{
rf_calculate_ei30(storms, settings = NULL, interval_min = NULL)
}
\arguments{
\item{storms}{A rainfall-event data frame produced by
\code{\link[=rf_identify_storms]{rf_identify_storms()}}.}

\item{settings}{An \code{rf_settings} object created by \code{\link[=rf_settings]{rf_settings()}}.
If \code{NULL}, the settings stored in \code{storms} are used. If no stored
settings are available, default settings from \code{\link[=rf_settings]{rf_settings()}} are
used.}

\item{interval_min}{Positive finite number giving the temporal
resolution of the rainfall-event grid, in minutes. If \code{NULL}, the
\code{interval_min} attribute stored by \code{\link[=rf_identify_storms]{rf_identify_storms()}} is used.
The interval must divide 30 minutes exactly, and all requested
intensity durations must be exact multiples of the interval.}
}
\value{
A data frame of class \code{rf_events} with one row per rainfall
event. It contains:
\itemize{
\item \code{storm_id}: event identifier;
\item \code{event_start}: timestamp of the first event interval;
\item \code{event_end}: timestamp of the final event interval;
\item \code{duration_min}: represented event duration, in minutes;
\item \code{precip_mm}: total event precipitation, in mm;
\item \code{energy_mj_ha}: total rainfall kinetic energy, in MJ/ha;
\item \code{ei30}: event EI30 erosivity, in MJ mm/(ha h);
\item \code{omit_precip_condition}: result of the precipitation omission
condition, or \code{NA} when disabled;
\item \code{omit_intensity_condition}: result of the intensity omission
condition, or \code{NA} when disabled;
\item \code{omitted}: whether the event satisfies the configured omission
rule;
\item \code{erosive}: logical inverse of \code{omitted};
\item intensity columns named \verb{i<duration>_mm_h} for each duration
requested in \code{settings$intensity_durations_min}.
}

The returned object also stores \code{settings} and \code{interval_min}
attributes.

If \code{storms} contains no events, an empty \code{rf_events} object with the
corresponding output structure is returned.
}
\description{
Calculates rainfall amount, maximum rolling rainfall intensities,
kinetic energy, EI30 erosivity, and erosive-event classification for
rainfall events produced by \code{\link[=rf_identify_storms]{rf_identify_storms()}}.
}
\section{EI30 erosivity}{


Event rainfall erosivity is calculated as

\deqn{
EI_{30} = E \times I_{30}
}

where \code{E} is total event rainfall kinetic energy in MJ/ha and
\code{I30} is the maximum continuous 30-minute rainfall intensity in
mm/h. EI30 therefore has units of MJ mm/(ha h).

Maximum rainfall intensities are calculated with
\code{\link[=rf_max_intensity]{rf_max_intensity()}} using continuous rolling windows rather than
fixed clock-period blocks.
}

\section{Temporal resolution}{


Calculation of EI30 requires rainfall data capable of representing
an exact 30-minute intensity window. \code{interval_min} must therefore
divide 30 minutes exactly.

Every duration requested in \code{settings$intensity_durations_min} must
also be an exact multiple of \code{interval_min}.

Fixed-interval calculations have been validated against RIST 3.99.10
using 1-, 5-, 10-, 15-, and 30-minute rainfall data. Rainfall data
with intervals greater than 30 minutes cannot provide a genuine I30
and therefore cannot be used for EI30 calculation.

Temporal aggregation can change maximum intensity, kinetic energy,
and EI30 even when total rainfall is unchanged. Results calculated
from different source resolutions should therefore not be expected
to be identical.
}

\section{Event omission}{


Event omission is controlled by \code{\link[=rf_settings]{rf_settings()}}.

The precipitation criterion is enabled with \code{omit_precip} and is
satisfied when total event precipitation is strictly less than
\code{omit_precip_below_mm}.

The intensity criterion is enabled with \code{omit_intensity} and is
satisfied when the selected maximum rolling intensity is strictly
less than \code{omit_intensity_below_mm_h}. The intensity duration is
selected with \code{omit_intensity_duration_min}.

When both criteria are enabled:
\itemize{
\item \code{omit_logic = "all"} omits an event only when both conditions
are satisfied;
\item \code{omit_logic = "any"} omits an event when either condition is
satisfied.
}

Exact threshold values do not satisfy a criterion because the
comparisons are strict (\code{<}).

If only one omission criterion is enabled, only that criterion is
evaluated. If both \code{omit_precip} and \code{omit_intensity} are \code{FALSE},
no events are omitted. This corresponds to including all identified
rainfall events.

For disabled criteria, the corresponding
\code{omit_precip_condition} or \code{omit_intensity_condition} column is
returned as \code{NA}, indicating that the condition was not evaluated.
}

\section{Single-record events}{


RIST 3.99.10 was observed during fixed-interval validation to assign
zero kinetic energy and zero EI30 to events represented by only one
rainfall record.

Set \code{single_record_energy = "rist_zero"} in \code{\link[=rf_settings]{rf_settings()}} to
reproduce this behaviour. With the default
\code{single_record_energy = "calculate"}, the selected kinetic-energy
equation is applied normally.
}

\examples{
rainfall <- data.frame(
  datetime = as.POSIXct(
    "2025-01-01 00:00:00",
    tz = "UTC"
  ) +
    seq(
      from = 0,
      by = 60,
      length.out = 30
    ),
  precip_mm = rep(1, 30)
)

storms <- rf_identify_storms(
  rainfall,
  interval_min = 1
)

events <- rf_calculate_ei30(
  storms
)

events

}
