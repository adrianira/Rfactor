% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-rainfall.R
\name{rf_read_rainfall}
\alias{rf_read_rainfall}
\title{Read timestamped rainfall data}
\usage{
rf_read_rainfall(
  file,
  datetime_col,
  precip_col,
  tz = "UTC",
  expected_interval_min = 1,
  ambiguous = "error",
  nonexistent = "error",
  validate = TRUE
)
}
\arguments{
\item{file}{Path to a delimited precipitation file readable by
\code{\link[data.table:fread]{data.table::fread()}}.}

\item{datetime_col}{Name of the column containing rainfall
timestamps.}

\item{precip_col}{Name of the column containing precipitation
amounts in millimetres.}

\item{tz}{Time zone in which input timestamp values should be
interpreted. Default is \code{"UTC"}.}

\item{expected_interval_min}{Positive finite number giving the base
temporal resolution of the rainfall record, in minutes.
Default is \code{1}. Gaps consisting of positive whole multiples of
this interval are allowed.}

\item{ambiguous}{Handling of ambiguous local times, such as those
occurring during a daylight-saving-time transition. Passed to
\code{\link[clock:date_time_parse]{clock::date_time_parse()}}. Default is \code{"error"}.}

\item{nonexistent}{Handling of nonexistent local times, such as
those occurring during a daylight-saving-time transition. Passed
to \code{\link[clock:date_time_parse]{clock::date_time_parse()}}. Default is \code{"error"}.}

\item{validate}{Logical. If \code{TRUE}, the default, the standardized
rainfall record is checked with \code{\link[=rf_validate_rainfall]{rf_validate_rainfall()}}. If
\code{FALSE}, this validation step is skipped.}
}
\value{
A data frame with:
\itemize{
\item \code{datetime}: parsed rainfall timestamps as \code{POSIXct};
\item \code{precip_mm}: precipitation amounts in millimetres.
}

The returned object also stores the input time zone and declared
temporal resolution in the attributes \code{input_timezone} and
\code{expected_interval_min}.
}
\description{
Reads a delimited precipitation file and standardizes the selected
timestamp and precipitation columns to \code{datetime} and \code{precip_mm}.
}
\details{
The input file does not need to contain every expected time step.
Sparse rainfall records and other gaps between observations are
accepted, provided timestamp differences remain compatible with the
declared base temporal resolution.

The reader preserves the rainfall observations supplied by the user.
It does not insert missing time steps, interpolate precipitation,
estimate missing rainfall, or assess the climatological completeness
of the record.

Accepted timestamp formats are:
\itemize{
\item \code{"DD.MM.YYYY HH:MM:SS"};
\item \code{"DD.MM.YYYY HH:MM"};
\item \code{"YYYY-MM-DD HH:MM:SS"};
\item \code{"YYYY-MM-DD HH:MM"};
\item \code{"DD.MM.YYYY"};
\item \code{"YYYY-MM-DD"}.
}

Date-only values are interpreted as midnight (\code{00:00:00}) in the
time zone supplied through \code{tz}.

Input rows are sorted chronologically when necessary. A warning is
issued when sorting changes the original row order.
}
\examples{
rainfall_file <- tempfile(fileext = ".csv")

writeLines(
  c(
    "time,rain",
    "2025-01-01 00:00:00,0.0",
    "2025-01-01 00:05:00,0.4",
    "2025-01-01 00:10:00,0.2"
  ),
  rainfall_file
)

rainfall <- rf_read_rainfall(
  rainfall_file,
  datetime_col = "time",
  precip_col = "rain",
  tz = "UTC",
  expected_interval_min = 5
)

rainfall

}
