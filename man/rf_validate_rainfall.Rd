% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-rainfall.R
\name{rf_validate_rainfall}
\alias{rf_validate_rainfall}
\title{Validate a rainfall time series}
\usage{
rf_validate_rainfall(data, expected_interval_min = 1)
}
\arguments{
\item{data}{A data frame containing columns named \code{datetime} and
\code{precip_mm}.}

\item{expected_interval_min}{Positive finite number giving the base
temporal resolution of the rainfall record, in minutes.
Consecutive timestamp differences may be positive whole multiples
of this value. Default is \code{1}.}
}
\value{
\code{TRUE}, invisibly, when validation succeeds. An error is
raised when the rainfall record is invalid.
}
\description{
Checks the structure, precipitation values, timestamp order, and
alignment with a declared base temporal resolution before rainfall
erosivity calculations are performed.
}
\details{
The rainfall record does not need to be temporally complete.
Consecutive timestamps may be separated by gaps, provided each
separation is a positive whole multiple of
\code{expected_interval_min}.

For example, with \code{expected_interval_min = 1}, timestamp differences
of 1, 2, 10, or 120 minutes are valid, whereas a difference of
1.5 minutes is not.

Validation does not assess whether a rainfall record is
climatologically complete. Missing time intervals, months, or years
are not filled, interpolated, estimated, or otherwise modified.

The function requires:
\itemize{
\item a \code{POSIXct} \code{datetime} column;
\item a numeric \code{precip_mm} column;
\item no missing or duplicate timestamps;
\item strictly increasing timestamps;
\item finite, non-missing, non-negative precipitation values;
\item timestamp differences compatible with the declared base interval.
}
}
\examples{
rainfall <- data.frame(
  datetime = as.POSIXct(
    c(
      "2025-01-01 00:00:00",
      "2025-01-01 00:01:00",
      "2025-01-01 00:10:00"
    ),
    tz = "UTC"
  ),
  precip_mm = c(0, 0.2, 0.1)
)

# The nine-minute gap is allowed because it is a whole
# multiple of the one-minute base interval.
rf_validate_rainfall(
  rainfall,
  expected_interval_min = 1
)

}
